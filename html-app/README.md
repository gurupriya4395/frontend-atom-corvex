# html-app

This folder is the whole HTML / CSS / JS desk.

- `index.html` — page
- `css/` — styles
- `js/` — logic, globe, map, sample data

From the repo root: `npm run dev` then open http://localhost:5173
